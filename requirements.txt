# Python 3.12+. Default stack: Qdrant + local embeddings (BAAI/bge-m3).
numpy>=1.24
qdrant-client>=1.11
python-dotenv>=1.0

# Local embeddings (BGE-large-en-v1.5)
sentence-transformers>=2.6
torch>=2.1
transformers>=4.40

# Optional: OpenAI embeddings if EMBEDDING_PROVIDER=openai
openai>=1.40
tiktoken>=0.7
